# ============================================================
# Teleops Automation Suite — Python Dependencies
# ============================================================
#
# 👉 ONLY USING SOP AUTO-LOGGING? (Lightweight, 5-second setup):
#    pip install -r auto_logging/requirements.txt
#    (Or double-click: Install_AutoLogging.bat)
#
# 👉 USING AUTO-CLAIM BOT PICKER? (Includes OCR & Vision):
#    pip install -r auto_claim/requirements.txt
#    (Or double-click: Install_AutoClaim.bat)
#
# 👉 INSTALL ALL DEPENDENCIES FOR BOTH TOOLS:
#    pip install -r requirements.txt
# ============================================================

# ── [SOP Auto-Logging] Lightweight Dependencies ────────────
pyautogui>=0.9.54
Pillow>=10.0.0

# ── [Auto-Claim] Screen Capture, Vision & OCR ──────────────
mss>=10.2.0
numpy>=2.0.0
opencv-python>=4.8.0
easyocr>=1.7.2
rapidfuzz>=3.0.0

# ============================================================
# OPTIONAL: GPU Acceleration for EasyOCR (NVIDIA GPU required)
# ============================================================
# Run the following to install PyTorch CUDA:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# Then set "gpu": true under "ocr" in auto_claim/config.json
# ============================================================
