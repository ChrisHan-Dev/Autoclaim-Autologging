# ============================================================
# Teleops Automation Suite — Python Dependencies
# ============================================================
#
# 👉 BẠN CHỈ DÙNG SOP AUTO-LOGGING? (Cài 5 giây, siêu nhẹ):
#    pip install -r auto_logging/requirements.txt
#    (Hoặc click đúp file: Install_AutoLogging.bat)
#
# 👉 BẠN DÙNG AUTO-CLAIM NHẬN BOT? (Đầy đủ OCR & Vision):
#    pip install -r auto_claim/requirements.txt
#    (Hoặc click đúp file: Install_AutoClaim.bat)
#
# 👉 BẠN MUỐN CÀI TẤT CẢ THƯ VIỆN CHO CẢ 2 TOOL:
#    pip install -r requirements.txt
# ============================================================

# ── [SOP Auto-Logging] Lightweight Dependencies ────────────
pyautogui>=0.9.54
Pillow>=10.0.0

# ── [Auto-Claim] Screen Capture, Vision & OCR ──────────────
mss>=10.2.0
numpy>=2.0.0
opencv-python>=4.8.0
easyocr>=1.7.2
rapidfuzz>=3.0.0

# ============================================================
# TÙY CHỌN: Tăng tốc GPU cho EasyOCR (Nếu máy có GPU NVIDIA)
# ============================================================
# Chạy lệnh sau để cài PyTorch CUDA:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# Sau đó đổi "gpu": true trong auto_claim/config.json -> "ocr"
# ============================================================
